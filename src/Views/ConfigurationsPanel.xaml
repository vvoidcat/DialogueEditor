<UserControl x:Class="DialogueEditor.Views.ConfigurationsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogueEditor.Views"
             xmlns:common="clr-namespace:DialogueEditor.Views.Common"
             xmlns:controllers="clr-namespace:DialogueEditor.Controllers"
             mc:Ignorable="d" 
             x:Name="ConfigurationsPanelRoot"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <controllers:ConfigurationsPanelViewModel/>
    </UserControl.DataContext>

    <GroupBox Header="Configurations" BorderBrush="#FFD7E3E9">
        <StackPanel Margin="2">
            <common:FileSearchControl/>

            <Button Content="New Configuration" Margin="5"/>

            <ListBox BorderThickness="0">
                <Label Content="Dlg1"/>
                <Label Content="Dlg2"/>
                <Label Content="aaa"/>
            </ListBox>
        </StackPanel>
    </GroupBox>
</UserControl>
